{{> menu}}

<form class="ui stacked segment form" action="/trail/{{traillist.id}}/updatetrail/{{trail.id}}" method="POST">
  <div class="two fields">
    <div class="field">
      <label>Trail Name</label>
      <input placeholder="Title" type="text" name="title" value="{{trail.trailname}}">
    </div>
    <div class="field">
      <label>Terrain Description</label>
      <input placeholder="Description" type="text" name="description" value="{{trail.terraindescription}}">
    </div>
    <div class="field">
      <label>Start Point</label>
      <input placeholder="start" type="text" name="start" value="{{trail.startpoint}}">
    </div>
    <div class="field">
      <label>longitude</label>
      <input placeholder="00" type="number" name="longitude" value="{{trail.longitude}}">
    </div>
    <div class="field">
      <label>latitude</label>
      <input placeholder="00" type="number" name="latitude" value="{{trail.latitude}}">
    </div>
    <div class="field">
      <label>Distance KM</label>
      <input placeholder="00" type="number" name="distancekm" value="{{trail.distancekm}}">
    </div>
  </div>
  <button class="ui blue submit button">Update Trail</button>
</form>